# CDK Shop

本目录用于存放 TaoAi CDK 自动售卖站项目，包括商城源码、部署配置、运维脚本和项目文档。

当前线上站点：`https://shop.edujerry.icu`

## 安全约定

- 不提交服务器密码、SSH 私钥、TOTP Secret 或恢复码。
- 不提交支付宝应用私钥、数据库密码、JWT 密钥等生产凭据。
- 配置文件只保留脱敏模板，生产配置通过服务器受限文件或环境变量维护。
