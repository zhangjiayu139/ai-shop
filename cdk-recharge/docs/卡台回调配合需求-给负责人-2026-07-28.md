# 卡台回调配合需求（白标侧索引）

**正式文档（请发给卡台负责人）在 cardplatform 仓库：**

`cardplatform/docs/白标CDK回调与对账配合需求-给卡台负责人-2026-07-28.md`

## 摘要

白标**不以轮询做主同步**。请卡台提供：

1. **P0** 终态 Webhook：成功 + 失败 + 取消，payload 含脱敏卡、CDK 状态、金额、邮箱  
2. **P1** 进度 Webhook：`status`/`stage` 边沿变化（`gpt_direct.progress`）  
3. **P2** CDK 生命周期 / Open API 字段补齐（核对兜底）  

本仓库接收端已有：`POST /api/v1/webhooks/cardplatform`（验签 + 幂等入库）。卡台扩事件后，白标再解析落库展示，**不新增轮询同步任务**。
