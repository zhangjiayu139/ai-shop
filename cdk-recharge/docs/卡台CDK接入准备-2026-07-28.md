# 卡台 CDK 接入准备

> 日期：2026-07-28  
> 产品边界：本站 = **白标兑换壳**；发码 / 资金 / 开卡 = **卡台 Open API**。

---

## 1. 本站职责

| 能力 | 路径 |
|------|------|
| 安装向导 / 登录 | `/ops/setup`、`/ops/login` |
| 卡台 Key / 出口 IP | `/ops/integration` |
| 发码与列码 | `/ops/cdkeys` |
| 兑换对账 | `/ops/orders` |
| Webhook 接收 | `/ops/webhooks` + `POST /api/v1/webhooks/cardplatform` |
| 外观 / 审计 | `/ops/appearance`、`/ops/audit` |
| 公开兑换 | `/recharge` → `/api/v1/public/cdk/*` |
| Session 账单查询 | `/billing` |

---

## 2. 卡台接口（权威）

文档：`docs/zovocard-cdk-zh.md`、`docs/zovocard-openapi-zh.md`。

### 公开兑换（无需登录）

| 步骤 | 方法 | 路径 |
|------|------|------|
| 预览 | POST | `/api/v1/cdk/preview` |
| 预检 | POST | `/api/v1/cdk/preflight` |
| 兑换 | POST | `/api/v1/cdk/redeem` |
| 结果 | GET | `/api/v1/cdk/result?token=` |

Base 示例：`https://zovocard.com`

### 程序化发码 / 对账（Open API）

- Base：`https://zovocard.com/openapi/v1`
- Header：`X-API-Key: sk_…`
- 发码：`POST /gpt-direct/cdks`
- 列码：`GET /gpt-direct/cdks`
- 兑换订单：`GET /gpt-direct/cdk-orders`

本站配置（`site_settings` /「卡台接入」）：

- `card_api_base`
- `card_api_key`（只写，读脱敏）

---

## 3. 实现状态

| 项 | 状态 |
|----|------|
| 实时价格 / 余额 / 发码 / 列码 | ✅ |
| 公开兑换 BFF | ✅ |
| 兑换对账 UI（邮箱 / 卡 / 金额 / CDK 状态） | ✅ |
| Webhook 入库 | ✅ |
| Session 账单 | ✅ |

### 配置

`card_api_base` = `https://zovocard.com`  
`card_api_key` = `sk_…`  
出口 IP 需加入卡台白名单。

---

## 4. 安全注意

若曾把 `.env` / `.push.env` / 生产 JWT / 卡台 Key 推到公开仓库：

1. **立即轮换**所有泄露密钥（CF token、PAT、JWT_SECRET、卡台 sk、Webhook secret）。
2. 确认 `.gitignore` 含：`.env`、`.push.env`、`data/*.db`、`*.pem`。
3. 生产密钥只写服务器 `app.env`。
